// Vitest global test setup.
// Currently no jest-dom matchers are needed since Phase 0-3 tests are pure
// logic (engine/data pipeline). Add `@testing-library/jest-dom` here once
// component tests are introduced alongside the UI phases (7+).
export {};
